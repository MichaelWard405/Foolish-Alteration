#Api2

# 1. Window Placer
placer {
    xorigin = 5
    yorigin = 5
    xstep = 5
    ystep = 5
    children = true
}

# 2. Main Layout
layout {
    layer = "top"
    position = "bottom"

    grid {
        css = "* { -GtkWidget-hexpand: true; }"
        
        # Native taskbar call, no external widgets needed
        taskbar {
            rows = 1
            icons = true
            labels = true
            group = false
            sort = false
        }
    }
}
